package org.codeGen.core.util.dmo;
/**
 * author:$userName
 * descriptiond模板
 * richie code
 */
import org.springframework.stereotype.Service;

import com.richie.framework.dao.BaseDAO;

@Service
@SuppressWarnings("unchecked")
public class $stringUtils.capitaliseAll(${model.name})DAO extends BaseDAO {

	public $stringUtils.capitaliseAll(${model.name})DAO() {
		this.setNamespace("$stringUtils.capitaliseAll(${model.name})");
	}

}
